#include <stdio.h>
int main()
{
    int radius = 7;
    float pie = 3.14;
    printf("the area of this circle %f \n", pie * radius * radius);
    int height = 3;
    printf("the volume of cyclinder is %f \n", pie * radius * radius * height);

    return 0;
}
